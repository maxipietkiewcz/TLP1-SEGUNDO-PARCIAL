<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>Editar Reserva de Vuelo</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/css/bootstrap.min.css"
    />
  </head>
  <body>
    <div class="container">
      <h1>Editar Reserva de Vuelo</h1>

      <form action="/reservas/<%= reserva.id %>?_method=PUT" method="POST">
        <div class="mb-3">
          <label for="nombrePasajero" class="form-label"
            >Nombre del Pasajero:</label
          >
          <input
            type="text"
            class="form-control"
            name="nombrePasajero"
            id="nombrePasajero"
            value="<%= reserva.nombrePasajero %>"
            required
          />
        </div>

        <div class="mb-3">
          <label for="origen" class="form-label">Origen:</label>
          <input
            type="text"
            class="form-control"
            name="origen"
            id="origen"
            value="<%= reserva.origen %>"
            required
          />
        </div>

        <div class="mb-3">
          <label for="destino" class="form-label">Destino:</label>
          <input
            type="text"
            class="form-control"
            name="destino"
            id="destino"
            value="<%= reserva.destino %>"
            required
          />
        </div>

        <button type="submit" class="btn btn-primary">Guardar Cambios</button>
      </form>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/js/bootstrap.bundle.min.js"></script>
  </body>
</html>




